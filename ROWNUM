/* ROWNUM
WHERE句で使用することで取得レコード数に制限をかけられる。

詳細説明
ORDER BY句と同時に使用するとWHERE句が優先される。

補足説明
元々はセレクトしてきた結果セットのレコード番号のこと。
擬似列ともいう。
*/

//サンプル
/*
tableテーブルの全レコードから10行を取得する。
*/
SELECT * FROM table WHERE ROWNUM <= 10 ;